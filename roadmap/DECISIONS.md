# Decisions (append-only)

- 2026-06-10 · Engine install committed directly to `develop` as the bootstrap exception (no PR — there is no CI or manager before the engine exists). Everything after this goes through `feat/F-XXXX` PRs. (department)
- 2026-06-10 · QA deployment surface assumed **Vercel** (conventional for Next.js). Reversible until Phase 1 wires it. (department)
- 2026-06-10 · Database: **SQLite via Prisma** for dev per the product spec ("local-first" is the product's identity). How staging persists data on a serverless host is an open Phase 1 design point for `/groom` — options include Turso/libSQL or a container host; decide-and-document, don't block. (department)
- 2026-06-10 · E2E framework: **Playwright** (already the product spec's choice). (department)
- 2026-06-10 · Old root `GOAL.md`/`ROADMAP.md` (prior Antigravity AFK harness, superseded by this engine) archived to `docs/archive/antigravity-harness/`, not deleted. (department)
- 2026-06-10 · Product code lives under `src/` (`src/app`, `src/components`, `src/lib`), not root `app/` as the README sketch shows — `verify.sh` keys product mode (hard lint/test gates) off `src/` existing. README's directory section gets updated when the scaffold lands (F-0001). (groom)
- 2026-06-10 · Unit + E2E tests live in root `tests/` (matching the README sketch), not `src/` — the gate's per-file test-wiring guard scans `src/` and predates a glob runner; keeping tests outside `src/` is correct until Vitest replaces that guard via factory work. (groom)
- 2026-06-10 · `scripts/seed.ts` (gate-hardcoded path, guardrail surface) rewritten as a delegating shim: runs product-owned `npm run seed` when defined, **fails** in product mode without one (the template stub silently passed), stays green in template mode. Factory work by the orchestrator so no product feature ever needs `scripts/` scope. (groom)
- 2026-06-10 · Staging host assumption changed **Vercel → Fly.io** (container + persistent volume): Vercel's serverless filesystem can't persist the SQLite file that is the product's identity. Operator can override — asked in QUESTIONS.md; F-0004 ships config + guide only, no agent-executed deploy. (groom)
- 2026-06-10 · Tone profiles (6 personas) groomed at priority 2: spec'd in README's RAG section but named by no roadmap bullet; core answering flow (P1) doesn't need them. (groom)
- 2026-06-10 · Team features are no-auth in this phase: teammates are records (name/email), not logins — "local-first" spec, and auth is operator-visible scope. Revisit only if the operator asks. (groom)
- 2026-06-10 · README's pinned versions (Next.js 16, React 19, Tailwind 3, pdf-parse/mammoth/read-excel-file) are >3 months old by default — every feature description carries a P1 re-verify note for brief time rather than pinning now. (groom)
- 2026-06-10 · "Later" roadmap bullets (trust pipeline, original-format export, optional AI generation) deliberately NOT groomed — `/groom` scopes Now/Next only; Later depends on the recoverable claude-software-5 PRD and stays unscoped until promoted. (groom)
- 2026-06-11 · Crash recovery shipped via PR branch `fix/recover-groom-output`, not a direct push: `develop` is GitHub-protected (requires the `verify` check), so "push to develop immediately" was implemented as push-branch-then-PR — the commit is loss-proof on the remote either way. (recovery)
- 2026-06-11 · Dependabot PRs #5 (@types/node 25.9.2→25.9.3) and #4 (claude-code-action 1.0.142→1.0.143) merged: patch-level bumps, dev-only/CI-only surface, required `verify` check green. #4 landed via auto-merge after a branch update (protection requires up-to-date branches). (recovery)
- 2026-06-11 · Stale PRs #1 (cursor), #2 (agent-baseline), #3 (jules) closed without merging: all target `main` (now prohibited), all predate the repo purge (03c6b4a) and engine install (af92153); merging would resurrect retired-harness files. Each closed with a one-line explanation on the PR. (recovery)
- 2026-06-11 · Tailwind **v4.3.0 (CSS-first)** adopted over the README's "Tailwind 3" pin — P1 live verification showed v4 is current stable; config is `postcss.config.mjs` + `@theme` in globals.css, no tailwind.config file. README directory/stack section update still pending (groomed into a later doc pass). (work F-0001)
- 2026-06-11 · TypeScript kept at the engine's existing **^6.0.3** for the product app (official Next 16 scaffold pins ^5, floor 5.1, no hard peer block; the gate is green on TS6 — switching down would churn the engine for no failure). (work F-0001)
- 2026-06-11 · F-0001 dependency set recorded per security review: `lucide-react` (icons, product-spec choice) + Vitest toolchain (`vitest`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react|dom|jest-dom`, `vite-tsconfig-paths`). Seven `"latest"` specifiers flagged by security review were pinned to lockfile-resolved caret ranges (commit bec135b) before merge. (work F-0001)
- 2026-06-11 · `biome.json` linter scoped via `includes` to TS/JS file types: Biome 2.x CSS parser errors on Tailwind v4 `@theme`. Scoping, not weakening — `npm run lint` covers scripts/ + src/ (8 files), engine meta-gate `biome lint scripts` unchanged. (work F-0001)
- 2026-06-11 · Per-feature shipping pattern fixed: product code merges via the `feat/F-XXXX` PR; state flips (`--passes/--status done`), evidence files, brief, PROGRESS/DECISIONS/metrics ride a small follow-up **record PR** — `develop` is protected and `status: done` would be a lie before the feature PR merges. (work F-0001)
- 2026-06-11 · `scripts/seed.ts` synced to upstream template commit 91b184c (PR #11): `SEED_SHIM_ACTIVE` recursion sentinel + defensive package.json read. Behavior kept identical to upstream per the department brief; idiom-only local adaptations (F-0002 references in comments). (engine sync)
- 2026-06-11 · **Prisma 7.8.0** adopted (P1 live-verified current stable; released 2026-04-22) over legacy v6: driver-adapter architecture (`@prisma/adapter-better-sqlite3` + native better-sqlite3), root `prisma.config.ts` (added to F-0002 authorized_paths via `--paths`), seed hook in config (package.json#prisma is removed in v7). Generated TS client emitted to `prisma/generated/` — gitignored, rebuilt by our own `postinstall: prisma generate` — kept outside `src/` so `biome lint scripts src` never sees machine output. (work F-0002)
- 2026-06-11 · No `.env` file introduced: the SQLite URL defaults inline (`file:./prisma/dev.db`) in prisma.config.ts and src/lib/prisma.ts with `DATABASE_URL` env override — avoids secret-file surface (agents can't read `.env*` anyway) and keeps the gate shim's `/prod/i` refusal meaningful. (work F-0002)
- 2026-06-11 · F-0002 dependency set recorded per security review: `@prisma/client`, `@prisma/adapter-better-sqlite3`, `prisma` (all 7.8.0, same-version pinned), `tsx` (seed runner — current Prisma docs' choice; engine scripts stay on ts-node). npm 11 allow-scripts approvals, version-pinned in package.json: `prisma@7.8.0`, `@prisma/engines@7.8.0` (CLI engine download), `better-sqlite3@12.10.0` (native binding) — exactly the three required; `sharp`/`esbuild`/`fsevents` deliberately left unapproved (they ship working prebuilts). Provenance of all new lockfile entries verified against registry.npmjs.org. (work F-0002)
- 2026-06-11 · F-0002 schema built by the standard `builder`, not `db-engineer`: greenfield `prisma db push` prototyping with no migration files or existing data — database.md's substantive rules (dev-only DBs, test per schema change) were folded into the brief instead. Revisit when real migrations start. (work F-0002)
- 2026-06-11 · CI `verify` red once on PR #15 from pure infra: the shellcheck npm package downloads its binary from GitHub releases at runtime and hit a 403 rate limit on the shared runner; rerun green with zero code change. Kaizen candidate if it recurs: vendor/cache the binary in CI. (work F-0002)
- 2026-06-11 · F-0003 dependency recorded: `@playwright/test` ^1.60.0 (live-verified current stable) — zero npm install scripts, so NO new allowScripts entries (fsevents transitive dep is darwin-only). Browser binaries arrive only via the explicit `playwright install --with-deps --only-shell chromium` inside the `e2e` script — self-sufficient because e2e.yml deliberately installs no browsers. (work F-0003)
- 2026-06-11 · E2E webServer uses the DEV server (`npm run dev -- -p 3100`, 120s timeout — Vercel's own example shape) over the Next docs' prod-build recommendation: standalone `npm run e2e` must reflect current source without requiring a fresh `.next`, and inside the gate the build has already passed. Port 3100 so an operator's dev server on 3000 never collides. Revisit toward prod-serve when F-0004 lands `npm run start` semantics. (work F-0003)
- 2026-06-11 · `.github/workflows/e2e.yml` brought to gate parity with ci.yml (PR #18, security APPROVE) after its first real run failed with every e2e step green: verify.sh hard-requires actionlint under CI=true and the pre-product workflow never installed it. Also: Node 24 parity, fetch-depth 0 (assertion shield diffs develop), trigger paths += tests/**, package.json, package-lock.json (test-only/dep-only PRs must not skip e2e). Engine lesson: a workflow that has never executed its real path is unverified — exercise it at the first opportunity. (work F-0003)
- 2026-06-11 · `next-env.d.ts` added to F-0003 authorized_paths post-hoc: Next's dev server auto-regenerates it (types path moved under `.next/dev/`) — a machine-managed artifact the builder cannot avoid touching; authorization follows reality. vitest.config.ts/next.config.ts were pre-authorized at brief time for the same class of reason (glob exclusion / native-module escape hatch). (work F-0003)
- 2026-06-11 · CLAUDE.md §6 += upstream CI-watch rule, verbatim (engine sync @ 8ab68ff, PR #21). §9 checklist walked: session/PR protocol belongs in the constitution (not path-scoped); no duplicate — §3.7's "On green CI: merge" lacked the watch-or-handoff requirement; 48/150 lines after edit; wording is an enforceable instruction (named command + named artifact). (engine sync)
- 2026-06-11 · e2e.yml reconciled to upstream F-0014 byte-identical (PR #21): our PR #18 fixed the same lane-parity defect independently with identical behavior, so the upstream shape was preferred per the diff-clean rule; upstream additionally triggers the workflow on its own file. Nothing in our shape was strictly better — no upstream candidate to report. (engine sync)
- 2026-06-11 · test-hooks.sh + update-state.ts synced verbatim from template @ 8ab68ff (PR #21), install-into.sh section excluded (template-only tooling). Standing translation note: PR numbers and DECISIONS dates inside comments of synced engine files (e.g. "incident PR #24", "DECISIONS 2026-06-10") refer to the TEMPLATE repo's history, not this repo's — kept verbatim so future syncs diff clean. (engine sync)
- 2026-06-11 · F-0004 enables `output: "standalone"` for a self-contained container; `npm run start` (= `next start`) kept unchanged — it still serves the full `.next` production build locally (the standalone warning is advisory), while the shipped container runs `node server.js` from `.next/standalone` via the Dockerfile CMD. Resolves F-0003's "revisit npm run start semantics" note: two valid serve paths for two contexts, no script change needed. (work F-0004)
- 2026-06-11 · Staging persistence pattern: a **synthetic** seed db is baked into the image at build (`npm run seed`) and the container CMD copies it onto the Fly `/data` volume only on first boot (`if [ ! -f ]`), so the SQLite file lives on the persistent volume and staging is populated without shipping the Prisma CLI/migration engine into the runtime image. `DATABASE_URL=file:/data/answerflow.db`; the deploy-config test pins `/data` as a prefix of that path. (work F-0004)
- 2026-06-11 · Fly.io confirmed as the staging host in shipped config (Vercel can't persist the single-file SQLite db that is the product's identity — prior DECISIONS 2026-06-10). F-0004 ships config + `docs/DEPLOY.md` guide only; the operator executes the deploy (their account + card). The QUESTIONS.md hosting item stays open — operator may still redirect to Render/Railway with a small config change. (work F-0004)
- 2026-06-11 · Security review (APPROVE) finding dispositions, F-0004: #1 (`.dockerignore` lacked `.env*`/key exclusions while the builder uses `COPY . .`) applied in-PR — added `.env`, `.env.*`, `*.pem`, `*.key`, `*.crt`, `*.p12`; #2 (runner has no non-root `USER`) **deferred** — a non-root process can't write the root-owned Fly volume without a runtime chown, and the first-boot `mkdir`/`cp` complicates it; low value for a DB-free public endpoint on staging. Revisit before any production (non-staging) reuse of this Dockerfile. (work F-0004)
- 2026-06-11 · shellcheck-binary 403 flake **recurred** on PR #22 verify (2nd occurrence; 1st was PR #15) — the `shellcheck` npm package downloads its binary from GitHub at gate time and hits rate limits on shared runners. Promoted from "candidate" to the **next kaizen**: vendor or cache the binary (cached actions step / authenticated fetch / pinned vendored binary) so dependency-touching features stop eating red CI rounds. (work F-0004)
